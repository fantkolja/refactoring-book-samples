/*
 	senseless code duplication
 */

// add the user to the database
addUserToDB();

/*
 	obsolete comments
 */

// add the user to the database
sendEmailUpdateNotification()

/*
 	commented out code
 */

// addUserToDB();

/*
 	answer to the question, WHAT is going on
 */

// calculate taxes
const tax = income * 0.05 + fee - 1000;

/*
 	answer to the question, WHY the code looks like that
 */

// starting from 01.01.2024 the government annulled tax deduction
const updatedTax = year < 2024 ? tax -1000 : tax;
